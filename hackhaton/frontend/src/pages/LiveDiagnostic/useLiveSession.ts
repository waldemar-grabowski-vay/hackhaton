/**
 * useLiveSession (T025 — Phase 3 / US1 stub).
 *
 * React hook that opens the WebSocket against
 * `/api/live/{host_id}/ws?developer_mode_check=1`, parses incoming
 * envelopes via `parseServerEnvelope`, exposes the live session state,
 * and dispatches outbound control envelopes. Implementation lands in T025.
 */
export {};
