PyQt6>=6.6
paramiko>=3.4
cantools>=39.4
# python-can is technically a cantools dependency but we use it directly
# for ASC recording, so pin it explicitly.
python-can>=4.3
# pyqtgraph is optional; only needed if you want the live trend plot in the
# right-hand panel. The POC degrades gracefully if it isn't installed.
pyqtgraph>=0.13
